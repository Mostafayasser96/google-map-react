import React from "react";


const DrawingManager = () => {
   const drawingManger = new google.maps.drawing.DrawingManager({
    drawingControlOptions: {
      
    }
   })
}
export default DrawingManager;